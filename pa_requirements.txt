# ─── GIL CLINIC — PythonAnywhere (slim) ──────────────────────────────────────
# Sirf runtime mein actually import hone wali dependencies.
# Full list requirements.txt mein hai (local/VM ke liye); PA ke 512MB disk
# ke liye heavy packages (google-generativeai, supabase, pandas, reportlab,
# edge-tts, asyncpg, openai/anthropic/groq client libs) nikale gaye —
# AI engine HTTP (requests) se providers ko call karta hai, isliye client
# packages ki zaroorat nahi.
fastapi>=0.111.0
pydantic-settings>=2.0.0
uvicorn>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.4,<3.2
itsdangerous>=2.1.0
pyjwt>=2.8.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
redis>=5.0.0
fpdf2>=2.7.0
qrcode[pil]>=7.4.0
pillow>=10.0.0
cryptography>=42.0.0
bcrypt>=4.1.0
APScheduler>=3.10.0
numpy>=1.24.0
