# Environment
.env
*.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
venv/
.venv/

# IDE
.vscode/
.idea/

# Secrets
pwa/secret.txt
secret.txt
admin_credentials.txt

# Backups & runtime junk (patient data — NEVER commit)
backups/
*.log
.deps_installed
track_debug.log
pa_data_upload.zip
pa_token.txt
pa_state.json

# Build stamp — deploy ke waqt pa_deploy.py apne aap banata + upload karta hai
build_info.json

# OS
.DS_Store
Thumbs.db
nul

# Cloudflare tunnel binary (bada 54MB — GitHub par nahi)
cloudflared.exe

# ZCode
.zcode/

# GHOS (separate project)
ghos/
ghos_memory/

# Node / Playwright (browser automation deps — not part of Python app)
node_modules/
package-lock.json
package.json
repo-analysis

# Dev scratch (test scripts, debug output)
scratch/
