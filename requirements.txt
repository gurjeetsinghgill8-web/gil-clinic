# ─── CardioQueue (Streamlit App) — Production Dependencies ──────────────────
# Install:  pip install -r requirements.txt
# ────────────────────────────────────────────────────────────────────────────

streamlit>=1.36.0
streamlit-autorefresh>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
requests>=2.31.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0

# UI / Charts
plotly>=5.0.0
altair>=5.0.0
tabulate>=0.9.0

# File Export
openpyxl>=3.0.0
fpdf2>=2.7.0
xlsxwriter>=3.0.0
qrcode[pil]>=7.4.0
pillow>=10.0.0
reportlab>=4.0.0

# Security
cryptography>=42.0.0
bcrypt>=4.1.0

# Notifications
pywhatkit>=5.0
APScheduler>=3.10.0

# AI Integrations (optional — comment out if not needed)
openai>=1.0.0
anthropic>=0.30.0
google-generativeai>=0.3.0
groq>=1.0.0

# Voice / Audio (optional)
edge-tts>=6.0.0

# Supabase (optional — for cloud database mode)
supabase>=2.0.0
httpx>=0.27.0
