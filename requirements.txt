# ─── CardioQueue (FastAPI App) — Production Dependencies ─────────────────────
# Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework
fastapi>=0.111.0
pydantic-settings>=2.0.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.2,<3.2
itsdangerous>=2.1.0

# Data
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
requests>=2.31.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0

# Redis (for Railway deployments)
redis>=5.0.0

# File Export
openpyxl>=3.0.0
fpdf2>=2.7.0
xlsxwriter>=3.0.0
qrcode[pil]>=7.4.0
pillow>=10.0.0
reportlab>=4.0.0
tabulate>=0.9.0

# Security
cryptography>=42.0.0
bcrypt>=4.1.0

# Notifications
APScheduler>=3.10.0

# AI Integrations (optional — comment out if not needed)
openai>=1.0.0
anthropic>=0.30.0
google-generativeai>=0.3.0
groq>=1.0.0

# Voice / Audio (optional)
edge-tts>=6.0.0

# Supabase (optional — for cloud database mode)
supabase>=2.0.0
httpx>=0.27.0
